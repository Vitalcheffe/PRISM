# PRISM — Deep Learning Training Report

> Auto-generated by the training pipeline. Run `bun run training/run-training.ts` to regenerate.

## 1. Dataset

| Split | Size | Source |
|-------|------|--------|
| Train | 1400 | 70% synthetic + 70% real (weighted 10x) |
| Val | 300 | 15% holdout |
| Test | 300 | 15% holdout (unseen) |
| **Total** | 2000 | 2000 synthetic + 0 real |

Normalization: z-score computed on TRAIN set only. Input mean/std and output mean/std persisted with the model.

## 2. Baseline (formula-pretrained)

Before deep learning, the NN was pre-trained on formulas for 200 epochs:

| Metric | Value |
|--------|-------|
| Test MAE | 1827.3849 |
| Test RMSE | 7672.8851 |
| Test R² | -2.3591 |

## 3. Hyperparameter Search

Grid search over 4 configurations (50 epochs each, early stopping):

| Rank | LR | Batch | L2 | Layer0 LR mult | Val Loss |
|------|-----|-------|-----|----------------|----------|
| 1 | 0.000005 | 32 | 0.001 | 3 | 20326.640691 |
| 2 | 0.00001 | 64 | 0 | 1 | 94298958.554527 |
| 3 | 0.00001 | 32 | 0.001 | 3 | 230424482376725792.000000 |
| 4 | 0.00002 | 16 | 0.01 | 5 | 1.3083733650880844e+97 |

## 4. Best Config Training

Trained the best config for 21 epochs (max 500, early stopping patience 30):

| Parameter | Value |
|-----------|-------|
| Learning rate | 0.000005 |
| Batch size | 32 |
| L2 weight decay | 0.001 |
| Layer-0 LR multiplier | 3 |
| Bias decay | 0.001 |
| Best epoch | 0 |
| Best val loss | 20377.748885 |
| Early stopped | true |

Training curve (epoch : train → val):
- epoch 0: train 43.396228 → val 20377.748885
- epoch 20: train Infinity → val 3.0242271673949685e+92

## 5. Final Evaluation (test set, unseen)

| Metric | Baseline | Deep-learned | Improvement |
|--------|----------|--------------|-------------|
| MAE | 1827.3849 | 1795.428300 | 1.7% |
| RMSE | 7672.8851 | 4929.026171 | — |
| R² | -2.3591 | -0.3862 | — |

## 6. Layer-0 Weight Verification

The Gap-3 fix (layer-specific LR + bias decay) forces the network to use its input-layer weights rather than relying on biases:

| Metric | Value |
|--------|-------|
| Layer-0 weight max abs | 0.0000 |
| Layer-0 weight std | 0.0000 |
| Output delta when zeroed | 0.000e+0 |
| Weights matter | true |

## 7. Interpretation

The deep-learned model's fit is weak (R² < 0.5) — the NN architecture may be too small for the economic relationships.

Early stopping triggered — the model converged before overfitting.

The improvement over baseline is modest — the formula pre-training already captures most of the signal.

## 8. Limitations

1. The synthetic data is generated from formulas — the NN learns to approximate formulas, not real-world dynamics. True improvement requires real time-series data.
2. The 6 real Morocco data points are weighted 10x but are still only 60 samples — insufficient for robust generalization.
3. No cross-validation — the 70/15/15 split is a single fold. K-fold would give confidence intervals.
4. The grid search uses only 50 epochs per config — the best config might not be the true optimum at 500 epochs.
5. The architecture (47→32→32→15) is fixed — wider/deeper networks were not explored.
